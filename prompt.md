So I want to do an fun AI experiment and learning. 

The image I shared contains a list of all the projects which I want to build (this is part of a paid course xd)

All these projects will live inside this directory only

here are some instructions:

- create directories for all the projects
- research on these project take practical assumptions whenever in doubt
- store the research so following agent can pick it and create a plan of what to build
- keep in mind we are building these projects as not real world apps but from learning and fun point of view. LEARNING is the main goal here
- wherever you think use ultrathink mode to solve complex problems
- I prefer typescript as I am fluent in it so if you have a choice to make you can go with typescript. If you think python is only the correct choice to build a project then you can do so
- SPAWN a swarm of agents to do things in parallel, wherever possible always use SWARM of agents instead of sequential things
- SPAWN an agent for each project and then within that for each project you can spawn multiple subagents do implement things in parallel
- focus on correctness we dont need to build very big project or mega scope as part of v1 we can always re-iterate
- working project is must, develop a feedback loop code, run, test, if it fails then again fix bugs, run, test in the loop of subagent
- You are given full autonomous perms, do the changes, take assumptions no AskUserAQuestion is needed here
Show less